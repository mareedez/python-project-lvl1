name: linter
on: [push]
jobs:
  Wemake-styleguide-check:
    runs-on: ubuntu-latest
    steps:
      - name: Wemake styleguide
        uses: wemake-services/wemake-python-styleguide@0.15.2
      - name: Adding poetry
        run: |
        pip install poetry
        make install
      - name: Using linter
        run: |
        make lint